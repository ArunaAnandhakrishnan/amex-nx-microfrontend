import { NgModule } from '@angular/core';

/**
 * SharedUtilsModule – place services, guards, interceptors, and tokens here
 * that are consumed by two or more micro-frontend applications.
 * Examples: AuthService, JwtInterceptor, ShellToken.
 *
 * NOTE: OMS and online-account (NX-native) use standalone APIs.
 *       For those, import directly from this library's barrel (index.ts).
 */
@NgModule({})
export class SharedUtilsModule {}
