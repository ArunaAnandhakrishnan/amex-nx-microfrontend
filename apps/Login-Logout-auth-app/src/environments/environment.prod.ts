// NOTE: value intentionally kept identical to environment.ts to preserve
// existing behavior. Update apiBaseUrl here to the real production
// gateway URL when one is available.
export const environment = {
  production: true,
  apiBaseUrl: 'http://localhost:8080',
};
